{
  "name": "flight-booking-tracker",
  "version": "2.0.0",
  "description": "Complete MERN Flight Booking Tracker Application",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "start": "concurrently \"npm run prod --prefix backend\" \"npm start --prefix frontend\"",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "test": "mocha backend/test/**/*.test.js"
  },
  "devDependencies": {
    "chai": "^5.3.3",
    "concurrently": "^7.0.0",
    "mocha": "^11.7.6"
  },
  "author": "Flight Booking Tracker Team",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^7.2.0"
  }
}
